services:
  ml_backend_example:
    build:
      context: .
    volumes:
      - ./:/app:z
    ports:
      - "2003:2000"
    extra_hosts:
      - minio:host-gateway
    networks:
      - ml_network

networks:
  ml_network:
    name: ml_network
